name: Deploy to Firebase Hosting

on:
  push:
<<<<<<< HEAD
    branches:
      - main
=======
    branches: [ main ]
>>>>>>> e2216ce (chore: move into my-chat-app and build)
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: public/my-chat-app

    steps:
<<<<<<< HEAD
      - name: Checkout source code
=======
      - name: Checkout code
>>>>>>> e2216ce (chore: move into my-chat-app and build)
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
<<<<<<< HEAD
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build Next.js App
        run: npm run build

      - name: Deploy to Firebase Hosting
        run: npx firebase-tools@latest deploy --only hosting --project chat-f0752 --json
=======
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build Next.js App
        run: npm run build

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CHAT_F0752 }}
          channelId: live
          projectId: chat-f0752
>>>>>>> e2216ce (chore: move into my-chat-app and build)
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
